import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Camera, CheckCircle2, FileImage, Loader2, RotateCcw, Sparkles, UploadCloud } from "lucide-react";
import { useRef, useState } from "react";
import { toast } from "sonner";
import { PageHeader } from "./Shared";
import { Streamdown } from "streamdown";

export default function SolvePage() {
  const inputRef = useRef<HTMLInputElement>(null); const [preview, setPreview] = useState<string>(); const [payload, setPayload] = useState<{ data: string; mimeType: "image/jpeg" | "image/png" | "image/webp" }>(); const [answer, setAnswer] = useState("");
  const solve = trpc.solve.image.useMutation({ onSuccess: data => setAnswer(data.answer), onError: error => toast.error(error.message) });
  const readFile = async (file?: File) => { if (!file) return; if (!["image/jpeg", "image/png", "image/webp"].includes(file.type)) { toast.error("فایلی با این فرمت پشتیبانی نمی‌شود."); return; } if (file.size > 8_000_000) { toast.error("حجم فایل بیش از حد مجاز است."); return; } const reader = new FileReader(); reader.onload = () => { const result = String(reader.result); setPreview(result); setPayload({ data: result.split(",")[1] ?? "", mimeType: file.type as any }); }; reader.readAsDataURL(file); };
  return <div><PageHeader eyebrow="حل مسئله با تصویر" title="حل کن" description="از سؤال درسی عکس بگیر؛ مداد اول سؤال را می‌فهمد و بعد راه‌حل را روشن و مرحله‌به‌مرحله توضیح می‌دهد." /><div className="grid gap-5 lg:grid-cols-[.82fr_1.18fr]"><section className="rounded-[1.5rem] border border-[#dce8e9] bg-white p-5 sm:p-7"><div className="mb-5 flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[#fff0df] text-[#d77b35]"><Camera className="h-5 w-5" /></div><div><h2 className="font-black text-[#315a60]">عکس سؤال را بارگذاری کن</h2><p className="mt-1 text-xs text-[#8aa0a3]">JPG، PNG و WebP تا ۸ مگابایت</p></div></div>{preview ? <div className="overflow-hidden rounded-2xl border border-[#dce8e9] bg-[#f7fafb]"><img src={preview} alt="پیش‌نمایش سؤال بارگذاری‌شده" className="max-h-[390px] w-full object-contain" /><div className="flex gap-2 border-t border-[#edf2f2] p-3"><Button variant="outline" onClick={() => { setPreview(undefined); setPayload(undefined); setAnswer(""); }} className="h-10 rounded-xl text-xs"><RotateCcw className="ml-2 h-4 w-4" /> انتخاب دوباره</Button><Button onClick={() => payload && solve.mutate(payload)} disabled={!payload || solve.isPending} className="h-10 flex-1 rounded-xl bg-[#087f8c] text-xs font-bold hover:bg-[#076d78]">{solve.isPending ? <><Loader2 className="ml-2 h-4 w-4 animate-spin" /> در حال تحلیل...</> : <><Sparkles className="ml-2 h-4 w-4" /> حل مرحله‌به‌مرحله</>}</Button></div></div> : <button onClick={() => inputRef.current?.click()} className="flex min-h-[360px] w-full flex-col items-center justify-center rounded-2xl border-2 border-dashed border-[#c9e0df] bg-[#fbfdfd] p-6 text-center transition hover:border-[#71c4c0] hover:bg-[#f1fbfa]"><input ref={inputRef} type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={e => readFile(e.target.files?.[0])} /><div className="mb-5 flex h-16 w-16 items-center justify-center rounded-[1.3rem] bg-[#eaf8f6] text-[#087f8c]"><UploadCloud className="h-7 w-7" /></div><p className="font-black text-[#315a60]">برای انتخاب عکس کلیک کن</p><p className="mt-2 text-xs text-[#8aa0a3]">یا عکس را اینجا بکش و رها کن</p><div className="mt-5 flex items-center gap-2 text-[11px] text-[#a0afb1]"><FileImage className="h-3.5 w-3.5" /> تصویر واضح‌تر، پاسخ دقیق‌تر</div></button>}</section><section className="min-h-[500px] rounded-[1.5rem] border border-[#dce8e9] bg-white p-5 sm:p-7"><div className="mb-5 flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[#e1f4f2] text-[#087f8c]"><Sparkles className="h-5 w-5" /></div><div><h2 className="font-black text-[#315a60]">توضیح مداد</h2><p className="mt-1 text-xs text-[#8aa0a3]">درک سؤال، روش حل، پاسخ نهایی</p></div></div>{answer ? <div className="rounded-2xl bg-[#f4f8f8] p-5 text-sm leading-8 text-[#436368]"><div className="mb-5 flex items-center gap-2 text-xs font-black text-[#087f8c]"><CheckCircle2 className="h-4 w-4" /> پاسخ آماده است</div><Streamdown>{answer}</Streamdown></div> : <div className="flex min-h-[360px] flex-col items-center justify-center text-center"><div className="mb-4 flex h-14 w-14 items-center justify-center rounded-2xl bg-[#f4f8f8] text-[#9ab0b2]"><Sparkles className="h-6 w-6" /></div><p className="text-sm font-bold text-[#6f878a]">هنوز سؤالی برای تحلیل نداریم</p><p className="mt-2 max-w-xs text-xs leading-6 text-[#9aabad]">یک عکس واضح از سؤال بارگذاری کن تا توضیح کامل را اینجا ببینی.</p></div>}</section></div></div>;
}
