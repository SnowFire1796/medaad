import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { CalendarDays, CheckCircle2, Clock3, Loader2, Plus, Target } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { PageHeader, EmptyState } from "./Shared";

export default function PlannerPage() {
  const plans = trpc.planner.list.useQuery(undefined, { retry: false }); const create = trpc.planner.create.useMutation({ onSuccess: () => { plans.refetch(); setOpen(false); setTitle(""); setGoal(""); toast.success("برنامه مطالعه ذخیره شد."); }, onError: e => toast.error(e.message) }); const [open, setOpen] = useState(false); const [title, setTitle] = useState(""); const [goal, setGoal] = useState(""); const [subject, setSubject] = useState("ریاضی"); const [minutes, setMinutes] = useState("30");
  return <div><PageHeader eyebrow="مسیر شخصی تو" title="برنامه‌ریزی" description="هدف را کوچک و روشن کن؛ مداد مسیر انجامش را کنار تو نگه می‌دارد." action={<Button onClick={() => setOpen(true)} className="h-11 rounded-xl bg-[#087f8c] font-bold hover:bg-[#076d78]"><Plus className="ml-2 h-4 w-4" /> هدف جدید</Button>} />{plans.data?.length ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{plans.data.map(plan => <div key={plan.id} className="rounded-[1.35rem] border border-[#deeaeb] bg-white p-5"><div className="flex items-start justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#e3f4e9] text-[#4a9667]"><Target className="h-5 w-5" /></div><span className="rounded-full bg-[#eaf8f6] px-2.5 py-1 text-[10px] font-bold text-[#087f8c]">{plan.status === "active" ? "فعال" : "تمام‌شده"}</span></div><h3 className="mt-5 text-base font-black text-[#315a60]">{plan.title}</h3><p className="mt-2 text-xs leading-6 text-[#84999c]">{plan.goal || "هدف یادگیری ثبت‌شده"}</p><div className="mt-5 flex items-center gap-4 text-[11px] text-[#8aa0a3]"><span className="flex items-center gap-1.5"><BookIcon />{plan.subject || "مطالعه"}</span>{plan.dailyMinutes && <span className="flex items-center gap-1.5"><Clock3 className="h-3.5 w-3.5" />روزی {plan.dailyMinutes} دقیقه</span>}</div></div>)}</div> : <EmptyState title="هنوز هدفی نساخته‌ای" description="برای امتحان، مرور یک مبحث یا هر هدف درسی، یک برنامه کوچک بساز." actionLabel="ساخت اولین برنامه" />}{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#10363b]/25 p-4 backdrop-blur-sm"><div className="w-full max-w-lg rounded-[1.5rem] bg-white p-6 shadow-2xl"><div className="mb-5 flex items-center justify-between"><h2 className="text-lg font-black text-[#315a60]">هدف جدید</h2><button onClick={() => setOpen(false)} className="text-sm text-[#8aa0a3]">بستن</button></div><label className="mb-2 block text-xs font-bold text-[#819599]">عنوان هدف</label><input value={title} onChange={e => setTitle(e.target.value)} placeholder="مثلاً مرور فصل اول علوم" className="mb-4 h-11 w-full rounded-xl border border-[#dce8e9] px-3 text-sm outline-none focus:border-[#71c4c0]" /><label className="mb-2 block text-xs font-bold text-[#819599]">چرا این هدف مهم است؟</label><textarea value={goal} onChange={e => setGoal(e.target.value)} placeholder="یک توضیح کوتاه" className="mb-4 min-h-24 w-full rounded-xl border border-[#dce8e9] p-3 text-sm outline-none focus:border-[#71c4c0]" /><div className="grid gap-3 sm:grid-cols-2"><div><label className="mb-2 block text-xs font-bold text-[#819599]">درس</label><select value={subject} onChange={e => setSubject(e.target.value)} className="h-11 w-full rounded-xl border border-[#dce8e9] bg-white px-3 text-sm outline-none"><option>ریاضی</option><option>علوم</option><option>فارسی</option><option>انگلیسی</option></select></div><div><label className="mb-2 block text-xs font-bold text-[#819599]">دقیقه در روز</label><input type="number" value={minutes} onChange={e => setMinutes(e.target.value)} className="h-11 w-full rounded-xl border border-[#dce8e9] px-3 text-sm outline-none" /></div></div><Button onClick={() => create.mutate({ title, goal, subject, dailyMinutes: Number(minutes) })} disabled={title.length < 2 || create.isPending} className="mt-5 h-11 w-full rounded-xl bg-[#087f8c] font-bold hover:bg-[#076d78]">{create.isPending ? <Loader2 className="ml-2 h-4 w-4 animate-spin" /> : <CheckCircle2 className="ml-2 h-4 w-4" />} ذخیره برنامه</Button></div></div>}</div>;
}
function BookIcon() { return <CalendarDays className="h-3.5 w-3.5" />; }
